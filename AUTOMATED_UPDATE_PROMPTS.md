# 🚀 Automated Page Update Prompts

**Copy each prompt → Paste into Cursor AI → Wait for completion → Move to next**

---

## 📋 **PROMPT 1: Home Page (app.py)**

```
Update the home page (app.py) to use the new page components system:

1. Add import at the top (after existing imports, around line 27):
   from utils.page_components import apply_header_spacing, add_mobile_ready_badge, add_page_footer

2. Add apply_header_spacing() immediately after st.set_page_config() (after line 34):
   apply_header_spacing()

3. Find the sidebar section (around line 1500+) and add add_mobile_ready_badge() inside the with st.sidebar: block, near the end before the footer:
   add_mobile_ready_badge()

4. Find the footer section at the very end (around line 4180+) and replace any existing footer with:
   add_page_footer()

After completing, print: "✅ HOME PAGE COMPLETE - Ready to run Page 1 update and test when ready."
```

---

## 📋 **PROMPT 2: Page 1 - ROI by Measure**

```
Update pages/1_📊_ROI_by_Measure.py to use the new page components system:

1. Add import at the top (after existing imports):
   from utils.page_components import apply_header_spacing, add_mobile_ready_badge, add_page_footer

2. Find st.set_page_config() and add immediately after it:
   apply_header_spacing()

3. Find the sidebar section (with st.sidebar:) and add near the end:
   add_mobile_ready_badge()

4. Add at the very end of the file:
   add_page_footer()

After completing, print: "✅ PAGE 1 COMPLETE - Ready to run Page 2 update and test when ready."
```

---

## 📋 **PROMPT 3: Page 2 - Cost Per Closure**

```
Update pages/2_💰_Cost_Per_Closure.py to use the new page components system:

1. Add import at the top (after existing imports):
   from utils.page_components import apply_header_spacing, add_mobile_ready_badge, add_page_footer

2. Find st.set_page_config() and add immediately after it:
   apply_header_spacing()

3. Find the sidebar section (with st.sidebar:) and add near the end:
   add_mobile_ready_badge()

4. Add at the very end of the file:
   add_page_footer()

After completing, print: "✅ PAGE 2 COMPLETE - Ready to run Page 3 update and test when ready."
```

---

## 📋 **PROMPT 4: Page 3 - Monthly Trend**

```
Update pages/3_📈_Monthly_Trend.py to use the new page components system:

1. Add import at the top (after existing imports):
   from utils.page_components import apply_header_spacing, add_mobile_ready_badge, add_page_footer

2. Find st.set_page_config() and add immediately after it:
   apply_header_spacing()

3. Find the sidebar section (with st.sidebar:) and add near the end:
   add_mobile_ready_badge()

4. Add at the very end of the file:
   add_page_footer()

After completing, print: "✅ PAGE 3 COMPLETE - Ready to run Page 4 update and test when ready."
```

---

## 📋 **PROMPT 5: Page 4 - Budget Variance**

```
Update pages/4_💵_Budget_Variance.py to use the new page components system:

1. Add import at the top (after existing imports):
   from utils.page_components import apply_header_spacing, add_mobile_ready_badge, add_page_footer

2. Find st.set_page_config() and add immediately after it:
   apply_header_spacing()

3. Find the sidebar section (with st.sidebar:) and add near the end:
   add_mobile_ready_badge()

4. Add at the very end of the file:
   add_page_footer()

After completing, print: "✅ PAGE 4 COMPLETE - Ready to run Page 5 update and test when ready."
```

---

## 📋 **PROMPT 6: Page 5 - Cost Tier Comparison**

```
Update pages/5_🎯_Cost_Tier_Comparison.py to use the new page components system:

1. Add import at the top (after existing imports):
   from utils.page_components import apply_header_spacing, add_mobile_ready_badge, add_page_footer

2. Find st.set_page_config() and add immediately after it:
   apply_header_spacing()

3. Find the sidebar section (with st.sidebar:) and add near the end:
   add_mobile_ready_badge()

4. Add at the very end of the file:
   add_page_footer()

After completing, print: "✅ PAGE 5 COMPLETE - Ready to run Page 6 update and test when ready."
```

---

## 📋 **PROMPT 7: Page 6 - AI Executive Insights**

```
Update pages/6_🤖_AI_Executive_Insights.py to use the new page components system:

1. Add import at the top (after existing imports):
   from utils.page_components import apply_header_spacing, add_mobile_ready_badge, add_page_footer

2. Find st.set_page_config() and add immediately after it:
   apply_header_spacing()

3. Find the sidebar section (with st.sidebar:) and add near the end:
   add_mobile_ready_badge()

4. Add at the very end of the file:
   add_page_footer()

After completing, print: "✅ PAGE 6 COMPLETE - Ready to run Page 7 update and test when ready."
```

---

## 📋 **PROMPT 8: Page 7 - What-If Scenario Modeler**

```
Update pages/7_📊_What-If_Scenario_Modeler.py to use the new page components system:

1. Add import at the top (after existing imports):
   from utils.page_components import apply_header_spacing, add_mobile_ready_badge, add_page_footer

2. Find st.set_page_config() and add immediately after it:
   apply_header_spacing()

3. Find the sidebar section (with st.sidebar:) and add near the end:
   add_mobile_ready_badge()

4. Add at the very end of the file:
   add_page_footer()

After completing, print: "✅ PAGE 7 COMPLETE - Ready to run Page 8 update and test when ready."
```

---

## 📋 **PROMPT 9: Page 8 - AI Capabilities Demo**

```
Update pages/8_🎓_AI_Capabilities_Demo.py to use the new page components system:

1. Add import at the top (after existing imports):
   from utils.page_components import apply_header_spacing, add_mobile_ready_badge, add_page_footer

2. Find st.set_page_config() and add immediately after it:
   apply_header_spacing()

3. Find the sidebar section (with st.sidebar:) and add near the end:
   add_mobile_ready_badge()

4. Add at the very end of the file:
   add_page_footer()

After completing, print: "✅ PAGE 8 COMPLETE - Ready to run Page 8b update and test when ready."
```

---

## 📋 **PROMPT 10: Page 8b - Campaign Builder**

```
Update pages/8_📋_Campaign_Builder.py to use the new page components system:

1. Add import at the top (after existing imports):
   from utils.page_components import apply_header_spacing, add_mobile_ready_badge, add_page_footer

2. Find st.set_page_config() and add immediately after it:
   apply_header_spacing()

3. Find the sidebar section (with st.sidebar:) and add near the end:
   add_mobile_ready_badge()

4. Add at the very end of the file:
   add_page_footer()

After completing, print: "✅ PAGE 8b COMPLETE - Ready to run Page 9 update and test when ready."
```

---

## 📋 **PROMPT 11: Page 9 - Alert Center**

```
Update pages/9_🔔_Alert_Center.py to use the new page components system:

1. Add import at the top (after existing imports):
   from utils.page_components import apply_header_spacing, add_mobile_ready_badge, add_page_footer

2. Find st.set_page_config() and add immediately after it:
   apply_header_spacing()

3. Find the sidebar section (with st.sidebar:) and add near the end:
   add_mobile_ready_badge()

4. Add at the very end of the file:
   add_page_footer()

After completing, print: "✅ PAGE 9 COMPLETE - Ready to run Page 10 update and test when ready."
```

---

## 📋 **PROMPT 12: Page 10 - Historical Tracking**

```
Update pages/10_📈_Historical_Tracking.py to use the new page components system:

1. Add import at the top (after existing imports):
   from utils.page_components import apply_header_spacing, add_mobile_ready_badge, add_page_footer

2. Find st.set_page_config() and add immediately after it:
   apply_header_spacing()

3. Find the sidebar section (with st.sidebar:) and add near the end:
   add_mobile_ready_badge()

4. Add at the very end of the file:
   add_page_footer()

After completing, print: "✅ PAGE 10 COMPLETE - Ready to run Page 11 update and test when ready."
```

---

## 📋 **PROMPT 13: Page 11 - ROI Calculator**

```
Update pages/11_💰_ROI_Calculator.py to use the new page components system:

1. Add import at the top (after existing imports):
   from utils.page_components import apply_header_spacing, add_mobile_ready_badge, add_page_footer

2. Find st.set_page_config() and add immediately after it:
   apply_header_spacing()

3. Find the sidebar section (with st.sidebar:) and add near the end:
   add_mobile_ready_badge()

4. Add at the very end of the file:
   add_page_footer()

After completing, print: "✅ PAGE 11 COMPLETE - Ready to run Page 13 update and test when ready."
```

---

## 📋 **PROMPT 14: Page 13 - Measure Analysis**

```
Update pages/13_📋_Measure_Analysis.py to use the new page components system:

1. Add import at the top (after existing imports):
   from utils.page_components import apply_header_spacing, add_mobile_ready_badge, add_page_footer

2. Find st.set_page_config() and add immediately after it:
   apply_header_spacing()

3. Find the sidebar section (with st.sidebar:) and add near the end:
   add_mobile_ready_badge()

4. Add at the very end of the file:
   add_page_footer()

After completing, print: "✅ PAGE 13 COMPLETE - Ready to run Page 14 update and test when ready."
```

---

## 📋 **PROMPT 15: Page 14 - Star Rating Simulator**

```
Update pages/14_⭐_Star_Rating_Simulator.py to use the new page components system:

1. Add import at the top (after existing imports):
   from utils.page_components import apply_header_spacing, add_mobile_ready_badge, add_page_footer

2. Find st.set_page_config() and add immediately after it:
   apply_header_spacing()

3. Find the sidebar section (with st.sidebar:) and add near the end:
   add_mobile_ready_badge()

4. Add at the very end of the file:
   add_page_footer()

After completing, print: "✅ PAGE 14 COMPLETE - Ready to run Page 15 update and test when ready."
```

---

## 📋 **PROMPT 16: Page 15 - Gap Closure Workflow**

```
Update pages/15_🔄_Gap_Closure_Workflow.py to use the new page components system:

1. Add import at the top (after existing imports):
   from utils.page_components import apply_header_spacing, add_mobile_ready_badge, add_page_footer

2. Find st.set_page_config() and add immediately after it:
   apply_header_spacing()

3. Find the sidebar section (with st.sidebar:) and add near the end:
   add_mobile_ready_badge()

4. Add at the very end of the file:
   add_page_footer()

After completing, print: "✅ PAGE 15 COMPLETE - Ready to run Page 16 update and test when ready."
```

---

## 📋 **PROMPT 17: Page 16 - ML Gap Closure Predictions**

```
Update pages/16_🤖_ML_Gap_Closure_Predictions.py to use the new page components system:

1. Add import at the top (after existing imports):
   from utils.page_components import apply_header_spacing, add_mobile_ready_badge, add_page_footer

2. Find st.set_page_config() and add immediately after it:
   apply_header_spacing()

3. Find the sidebar section (with st.sidebar:) and add near the end:
   add_mobile_ready_badge()

4. Add at the very end of the file:
   add_page_footer()

After completing, print: "✅ PAGE 16 COMPLETE - Ready to run Page 17 update and test when ready."
```

---

## 📋 **PROMPT 18: Page 17 - Competitive Benchmarking**

```
Update pages/17_📊_Competitive_Benchmarking.py to use the new page components system:

1. Add import at the top (after existing imports):
   from utils.page_components import apply_header_spacing, add_mobile_ready_badge, add_page_footer

2. Find st.set_page_config() and add immediately after it:
   apply_header_spacing()

3. Find the sidebar section (with st.sidebar:) and add near the end:
   add_mobile_ready_badge()

4. Add at the very end of the file:
   add_page_footer()

After completing, print: "✅ PAGE 17 COMPLETE - Ready to run Page 18 update and test when ready."
```

---

## 📋 **PROMPT 19: Page 18 - Compliance Reporting**

```
Update pages/18_📋_Compliance_Reporting.py to use the new page components system:

1. Add import at the top (after existing imports):
   from utils.page_components import apply_header_spacing, add_mobile_ready_badge, add_page_footer

2. Find st.set_page_config() and add immediately after it:
   apply_header_spacing()

3. Find the sidebar section (with st.sidebar:) and add near the end:
   add_mobile_ready_badge()

4. Add at the very end of the file:
   add_page_footer()

After completing, print: "✅ PAGE 18 COMPLETE - Ready to run Page 18b update and test when ready."
```

---

## 📋 **PROMPT 20: Page 18b - Secure AI Chatbot**

```
Update pages/18_🤖_Secure_AI_Chatbot.py to use the new page components system:

1. Add import at the top (after existing imports):
   from utils.page_components import apply_header_spacing, add_mobile_ready_badge, add_page_footer

2. Find st.set_page_config() and add immediately after it:
   apply_header_spacing()

3. Find the sidebar section (with st.sidebar:) and add near the end:
   add_mobile_ready_badge()

4. Add at the very end of the file:
   add_page_footer()

After completing, print: "✅ PAGE 18b COMPLETE - Ready to run Page 19 update and test when ready."
```

---

## 📋 **PROMPT 21: Page 19 - Health Equity Index**

```
Update pages/19_⚖️_Health_Equity_Index.py to use the new page components system:

1. Add import at the top (after existing imports):
   from utils.page_components import apply_header_spacing, add_mobile_ready_badge, add_page_footer

2. Find st.set_page_config() and add immediately after it:
   apply_header_spacing()

3. Find the sidebar section (with st.sidebar:) and add near the end:
   add_mobile_ready_badge()

4. Add at the very end of the file:
   add_page_footer()

After completing, print: "✅ PAGE 19 COMPLETE - Ready to run Page Z update and test when ready."
```

---

## 📋 **PROMPT 22: Page Z - Performance Dashboard**

```
Update pages/z_Performance_Dashboard.py to use the new page components system:

1. Add import at the top (after existing imports):
   from utils.page_components import apply_header_spacing, add_mobile_ready_badge, add_page_footer

2. Find st.set_page_config() and add immediately after it:
   apply_header_spacing()

3. Find the sidebar section (with st.sidebar:) and add near the end:
   add_mobile_ready_badge()

4. Add at the very end of the file:
   add_page_footer()

After completing, print: "✅ PAGE Z COMPLETE - ALL 22 PAGES UPDATED! Ready for final verification."
```

---

## 🎯 **FINAL PROMPT: Verification & Deployment**

```
Verify all pages have been updated correctly:

1. Check that all 22 files have the import statement:
   from utils.page_components import apply_header_spacing, add_mobile_ready_badge, add_page_footer

2. Check that all pages call apply_header_spacing() after st.set_page_config()

3. Check that all pages have add_mobile_ready_badge() in their sidebar

4. Check that all pages have add_page_footer() at the end

5. Run a quick test:
   - Start Streamlit: streamlit run app.py --server.port 8502
   - Navigate through a few pages
   - Verify spacing looks good
   - Verify mobile badge appears in sidebar
   - Verify footer appears at bottom

6. If everything looks good, print:
   "✅ VERIFICATION COMPLETE - All 22 pages updated successfully! Ready for deployment."
```

---

## 📊 **Progress Tracker**

Use this to track your progress:

- [ ] PROMPT 1: Home Page (app.py)
- [ ] PROMPT 2: Page 1 - ROI by Measure
- [ ] PROMPT 3: Page 2 - Cost Per Closure
- [ ] PROMPT 4: Page 3 - Monthly Trend
- [ ] PROMPT 5: Page 4 - Budget Variance
- [ ] PROMPT 6: Page 5 - Cost Tier Comparison
- [ ] PROMPT 7: Page 6 - AI Executive Insights
- [ ] PROMPT 8: Page 7 - What-If Scenario Modeler
- [ ] PROMPT 9: Page 8 - AI Capabilities Demo
- [ ] PROMPT 10: Page 8b - Campaign Builder
- [ ] PROMPT 11: Page 9 - Alert Center
- [ ] PROMPT 12: Page 10 - Historical Tracking
- [ ] PROMPT 13: Page 11 - ROI Calculator
- [ ] PROMPT 14: Page 13 - Measure Analysis
- [ ] PROMPT 15: Page 14 - Star Rating Simulator
- [ ] PROMPT 16: Page 15 - Gap Closure Workflow
- [ ] PROMPT 17: Page 16 - ML Gap Closure Predictions
- [ ] PROMPT 18: Page 17 - Competitive Benchmarking
- [ ] PROMPT 19: Page 18 - Compliance Reporting
- [ ] PROMPT 20: Page 18b - Secure AI Chatbot
- [ ] PROMPT 21: Page 19 - Health Equity Index
- [ ] PROMPT 22: Page Z - Performance Dashboard
- [ ] FINAL: Verification & Deployment

---

## ⏱️ **Time Estimate**

- **Setup:** 5 minutes
- **Pages 1-6:** 20 minutes (Session 1)
- **Pages 7-13:** 20 minutes (Session 2)
- **Pages 14-20:** 20 minutes (Session 3)
- **Pages 21-Z:** 15 minutes (Session 4)
- **Final Verify:** 5 minutes
- **TOTAL:** ~85 minutes (~1.5 hours)

**Recommendation:** Do it in 4 sessions with breaks!

---

## 🎨 **What You'll Get**

After completing all prompts:

✅ **Consistent spacing** across all pages  
✅ **Mobile-ready badge** in every sidebar  
✅ **Professional footer** on every page  
✅ **HIPAA compliance messaging** throughout  
✅ **Ready for Streamlit Cloud deployment**

---

## 🚀 **Quick Start**

1. Open `AUTOMATED_UPDATE_PROMPTS.md`
2. Copy PROMPT 1
3. Paste into Cursor AI
4. Wait for completion message
5. Copy PROMPT 2
6. Repeat until all 22 prompts are done
7. Run FINAL verification
8. Deploy!

**That's it! Just copy → paste → wait → next!**

